import { TransformcolorDirective } from './transformcolor.directive';

describe('TransformcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new TransformcolorDirective();
    expect(directive).toBeTruthy();
  });
});
